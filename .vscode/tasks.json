{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "c.cpp",  //任务名，就是一开始创建的c.cpp
            "type": "shell",
            "command": "g++",
            "args": [   //其他参数
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.out",
            ], 
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}

